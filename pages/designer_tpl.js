import React from 'react-native';
const { View, Text } = React;

const styles = React.StyleSheet.create({
});

export default (page)=> {
    return (
        <View>
            <Text>Designer Page...</Text>
        </View>
    );
};
